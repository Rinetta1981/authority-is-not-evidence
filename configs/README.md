# Experiment Configurations

This directory contains configuration files defining experimental settings.

Configurations will record settings such as:

- model name and revision;
- random seed;
- dataset paths;
- batch size;
- experimental conditions;
- fine-tuning parameters;
- activation-analysis settings;
- intervention settings.

Keeping these values outside the main Python code helps make experiments easier to reproduce and audit.
